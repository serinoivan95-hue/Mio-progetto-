name: Deploy KERN to GitHub Pages

on:
  push:
    branches: ["claude/ciao-claude-1wurrl"]
    paths: ["kern/**"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install dependencies
        run: npm install
        working-directory: kern
      - name: Build
        run: npm run build
        working-directory: kern
      - name: Deploy to gh-pages branch
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: kern/dist
          branch: gh-pages
          clean: true
